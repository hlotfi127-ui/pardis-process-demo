# چک‌لیست پذیرش نسخه ۴ — سامانه مدیریت و پایش فرآیندهای پردیس پلاست کویر

این فایل معیار کنترل نهایی نسخه ۴ است. هیچ موردی نباید بدون تأیید اجرای واقعی، «تکمیل‌شده» تلقی شود.

## 1) صفحه اصلی و نقشه فرآیندها
- مالک برتر و پیشتازان بهبود اثربخش در بالای صفحه و قبل از نقشه فرآیندها.
- رتبه‌بندی فقط از داده واقعی؛ تعداد زیاد CAPA به‌عنوان امتیاز مثبت تلقی نشود.
- عنوان ثابت: «نقشه فرآیندها».
- سمت ورودی: «نیازها و الزامات مشتریان و ذی‌نفعان» به‌صورت ستون عمودی بزرگ.
- مرکز: فرآیندهای مدیریتی، اصلی و پشتیبانی با هویت بصری واضح.
- سمت خروجی: «رضایت مشتریان و ذی‌نفعان».
- فلش واضح ورودی ← فرآیندها ← رضایت و حلقه بازخورد/بهبود مستمر.
- کارت هر فرآیند: نام، کد، مالک، سمت، عکس/Avatar و لینک مستقیم به شناسنامه.

## 2) مالکان تأییدشده
- حمید تقوایی — مدیرعامل — PR-01 مدیریت استراتژیک و رهبری.
- حامد لطفی — مدیر سیستم کیفیت — PR-02 بهبود مستمر و PR-07 کنترل کیفیت.
- علی نمازی‌فر — مدیر منابع انسانی — PR-10 منابع انسانی.
- فرهاد محمدی — مدیر اجرایی شرکت — PR-04 طراحی و توسعه محصول، PR-05 برنامه‌ریزی تولید، PR-06 تولید و عملیات، PR-08 انبارش و تحویل، PR-12 مدیریت تجهیزات و ابزارآلات.
- یک شخص = یک پروفایل/شناسه؛ عکس و اطلاعات فرد در فرآیندهای مختلف تکرار داده‌ای نشود.
- برای افراد بدون عکس، Avatar استاندارد نمایش داده شود.

## 3) شناسنامه فرآیندها
- PR-01 و PR-02 مطابق فایل‌های نهایی کنترل‌شده؛ بدون حذف یا تغییر خودسرانه محتوا.
- سربرگ قوی شامل نام، کد، نوع، مالک، تاریخ و شماره بازنگری.
- عکس مالک بزرگ‌تر و سمت مشخص.
- جریان فرآیند در یک ردیف خوانا: از فرآیند/منبع ← ورودی‌ها ← فعالیت‌های کلیدی ← خروجی‌ها ← به فرآیند/مقصد.
- فعالیت‌های کلیدی از نظر بصری برجسته‌تر باشند.
- هر ردیف KPI دارای دکمه «پایش و اندازه‌گیری» باشد.
- بخش «مستندات مرتبط» برای روش/فرم‌های مرتبط.

## 4) ناوبری ثابت
- منوی فرآیندی ثابت: شناسنامه ← پایش و اندازه‌گیری ← تحلیل فرآیند ← CAPA ← ارزیابی اثربخشی.
- Breadcrumb در صفحات جزئیات.
- صفحات جزئیات: Back.
- فرم‌ها: Back + Cancel.
- Cancel در صورت تغییر ذخیره‌نشده، تأیید خروج بخواهد.
- Dead End وجود نداشته باشد.

## 5) مسیر عملیاتی اصلی
- Stepper ثابت: پایش → تحلیل فرآیند → CAPA → اجرای اقدام → ارزیابی اثربخشی → پایان.
- مرحله انجام‌شده سبز، مرحله جاری واضح، مراحل بعدی خاکستری.
- «تحلیل اثربخشی» در رابط کاربری به «تحلیل فرآیند» تغییر نام دهد.
- «ارزیابی اثربخشی اقدامات» مرحله مستقل بعد از اجرای اقدام باشد.

## 6) پایش و اندازه‌گیری
- جدول KPI + Sparkline زنده از DB.
- Sparkline و نمودار تفصیلی هیچ داده ساختگی تولید نکنند.
- در نبود داده کافی: «داده کافی برای نمایش روند وجود ندارد».
- نمودار تفصیلی شامل داده واقعی، خط حد پذیرش، ماه‌ها، عنوان، واحد، جهت و سال.
- رنگ وضعیت‌ها پررنگ و خوانا؛ فقط متکی به رنگ نباشد.
- ماه‌های غیرسررسید قابل ویرایش نباشند.
- تب‌ها: نمای کلی | ثبت نتایج | نمودارها | سوابق پایش.
- متن راهنما: «برای ثبت نتیجه، روی ماه سررسید شاخص کلیک کنید.»

## 7) یکپارچگی زمان ثبت
- فیلد دستی «ثبت در موعد مقرر؟» حذف شود.
- دوره عملکرد، موعد ثبت و زمان واقعی ثبت جدا باشند.
- زمان واقعی ثبت از Timestamp سیستم/سرور.
- وضعیت‌ها خودکار: ثبت به‌موقع | ثبت با تأخیر | سررسیدشده و ثبت‌نشده | هنوز سررسید نشده.
- KPI تحویل به‌موقع گزارش‌ها فقط از Timestamp واقعی محاسبه شود.
- هر اصلاح مدیریتی حساس با Audit Trail: مقدار قبل/بعد، کاربر، زمان و دلیل.

## 8) تحلیل فرآیند
- برای KPI سررسیدشده تحلیل انجام شود حتی اگر حد پذیرش محقق شده باشد.
- بررسی حد پذیرش + روند حداقل سه دوره (در صورت وجود) + عوامل مؤثر + نتیجه + تصمیم.
- پس از کنترل صحت داده، عدم تحقق حد پذیرش → CAPA اجباری.
- با وجود تحقق حد پذیرش، در روند نامطلوب/تکرار/رویداد مهم امکان CAPA وجود داشته باشد.
- CAPA به KPI و دوره تحلیل متصل باشد.

## 9) CAPA
- چرخه: مسئله → علت ریشه‌ای → تعریف اقدامات → اجرای اقدامات → ارزیابی اثربخشی → بستن/اقدام تکمیلی.
- یک CAPA می‌تواند چند اقدام داشته باشد.
- مسئول اقدام از کاربران مجاز انتخاب شود، نه متن آزاد.
- اقدام دارای موعد انجام و موعد ارزیابی اثربخشی باشد.
- امکان بستن فقط بعد از تکمیل شروط و تأیید اثربخشی.
- ارتباط CAPA با KPI، فرآیند و دوره تحلیل حفظ شود.

## 10) ارزیابی اثربخشی اقدامات
- ارزیابی مستقل توسط QA طبق مرجع کنترل‌شده.
- نتیجه: اثربخش / غیراثربخش.
- غیراثربخش → اقدام تکمیلی یا بازگشایی.
- بهبود فقط وقتی محسوب شود که اجرا و اثربخشی آن تأیید شده باشد.

## 11) اعلان‌ها
- مرکز اعلان داخل نرم‌افزار.
- هنگام تخصیص اقدام، اعلان برای مسئول.
- ساختار برای یادآوری قبل از سررسید، روز سررسید و تأخیر آماده باشد.
- KPI سررسیدشده/تحلیل معوق/اثربخشی آماده ارزیابی قابلیت اعلان داشته باشند.

## 12) روش اجرایی و راهنمای درون‌سامانه‌ای
- صفحه استاندارد روش اجرایی: کد، عنوان، بازنگری، هدف، دامنه، تعاریف، مسئولیت‌ها، جریان، مراحل، مدارک/فرم‌ها و سوابق.
- در صفحه عملیاتی فقط بند و مدرک مرتبط همان مرحله نمایش داده شود.
- نگاشت مرحله ↔ روش اجرایی ↔ بند ↔ فرم/رکورد ↔ بازنگری به‌صورت داده‌ای و قابل نگهداری طراحی شود.
- کد فرم/مدرک از مستندات کنترل‌شده گرفته شود؛ هیچ کد حدسی وارد نشود.
- ماژول مستقل «ریسک» ایجاد نشود.

## 13) داشبورد بهبود مستمر
- تعداد CAPA، تعداد اقدامات، تکمیل‌شده، باز، سررسیدگذشته، منتظر اثربخشی، بهبود اثربخش و درصد تکمیل.
- رتبه فرآیند/مالک براساس بهبود اثربخش؛ با عکس مالک در صورت وجود.
- فیلتر دوره، فرآیند، مالک و معیار.

## 14) Backup / Audit / معماری آینده
- Backup پایگاه داده، صفحه وضعیت Backup و Backup دستی.
- بازیابی فقط برای Administrator و در نسخه نهایی با کنترل کافی.
- حداقل یک توصیه صریح برای نگهداری نسخه خارج از همان دیسک.
- Audit Trail برای ویرایش‌های حساس.
- اشخاص، فرآیندها، KPIها، CAPAها، اقدامات و مستندات با ID پایدار و روابط واقعی.
- ساختار آماده API و مهاجرت بعدی SQLite → PostgreSQL بدون بازورود اطلاعات.

## 15) هویت بصری
- Navy = ساختار/مدیریت؛ Orange = هویت پردیس/تأکید؛ Green/Red/Blue = وضعیت عملکرد.
- کنتراست بالاتر از نسخه قبلی؛ آیکون‌های واضح‌تر و کارت‌های قوی‌تر.
- صفحه ورود حرفه‌ای با تصویرسازی خطی محصولات پردیس و رنگ نارنجی؛ صفحات عملیاتی خلوت بمانند.

## 16) تست قبل از تحویل ZIP
- اجرای Node.js و ایجاد/بازشدن SQLite.
- GET و HEAD صفحه login بدون خطای 500.
- Login/Logout.
- همه منوها و لینک‌ها بدون 404/500.
- ثبت KPI و محاسبه Timestamp/On-time.
- تغییر داده و به‌روزرسانی Sparkline/Chart.
- تحلیل و صدور CAPA.
- ایجاد چند اقدام و تخصیص مسئول/اعلان.
- ثبت ارزیابی اثربخشی و کنترل بستن CAPA.
- ایجاد Backup و کنترل باقی‌ماندن داده پس از Restart.
- ZIP مستقل نسخه ۴ همراه START_DASHBOARD.bat و README.
